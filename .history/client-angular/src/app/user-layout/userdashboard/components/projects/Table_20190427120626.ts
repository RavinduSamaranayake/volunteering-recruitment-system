export interface Table {
    name: String; 
    title: String;
    description: String;
    date: String;
    attendees: String;
    rating: req.body.rating,
    image: req.body.image,
    organization: req.body.organization
}
