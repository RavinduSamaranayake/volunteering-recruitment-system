export interface Table {
    name: String; 
    title: String;
    description: String;
    date: String;
    attendees: String;
    rating: String;
    image: req.body.image,
    organization: req.body.organization
}
